Esta es una guía detallada paso a paso para resolver la "Práctica 3. Escalado Vertical con RDS"  basándonos en el documento proporcionado por el IES Juan de Garay.

---

# Guía Paso a Paso: Práctica 3 - Escalado Vertical en Amazon RDS

Esta práctica tiene como objetivo aprender a modificar la capacidad de proceso (CPU y RAM) de una base de datos relacional.

### Parte 1: Creación de la Instancia RDS

1. **Accede a la consola de AWS** y busca el servicio **RDS**.
2. Haz clic en **"Crear base de datos"**.
3. 
**Selección del Motor:** Elige **MySQL** o **MariaDB** tal como indica el enunciado.


4. **Selección de la Instancia (Estado Inicial):**
* En la sección de "Clase de instancia", selecciona una opción básica (por ejemplo, `db.t3.micro` o `db.t2.micro` si estás en la capa gratuita).
* 
**Tarea para el informe:** Anota el nombre de la instancia (ej. `db.t3.micro`) y busca sus características (vCPU y RAM) en la documentación de AWS o en el enlace que sugiere la práctica.




5. **Configuración de Almacenamiento:**
* Busca la sección de "Almacenamiento".
* Asegúrate de que la casilla **"Habilitar escalado automático de almacenamiento"** esté marcada (suele ser el valor por defecto).
* 📷 **CAPTURA 1:** Toma una captura de esta sección donde se vea el almacenamiento asignado y la casilla de autoescalado marcada.





---

### Parte 2: Escalado Vertical (Modificación)

Una vez la base de datos esté en estado "Disponible", procederemos a cambiarla por una más potente.

1. Selecciona tu instancia y haz clic en el botón **"Modificar"**.
2. **Cambio de Instancia:**
* Busca la sección "Clase de instancia".
* Cambia el tipo a uno superior (por ejemplo, si tenías `db.t3.micro`, cambia a `db.t3.small` o `db.t3.medium`).


* 
**Tarea para el informe:** Anota el nuevo nombre de la instancia y sus nuevas características de CPU y RAM.




3. **Aplicar Cambios:**
* Ve al final de la página y dale a "Continuar".
* En el resumen de modificaciones, selecciona **"Aplicar inmediatamente"** (Apply Immediately).


* Haz clic en "Modificar la instancia de base de datos".



---

### Parte 3: Análisis Teórico (Respuestas para tu práctica)

Debes responder a estas preguntas en tu documento basándote en lo observado:

* 
**¿Qué ocurre al guardar los cambios?** 


* *Respuesta sugerida:* La instancia entra en estado "Modifying" (Modificando). Al ser un escalado vertical (cambio de hardware virtual), la base de datos suele requerir un reinicio o una breve interrupción del servicio para reasignar los nuevos recursos (CPU/RAM).


* 
**En un entorno real, ¿es mejor "inmediatamente" o en "ventana de mantenimiento"?** 


* *Respuesta sugerida:* En un entorno real de producción, es mejor hacerlo en la **siguiente ventana de mantenimiento**. Hacerlo "inmediatamente" provoca una caída del servicio (downtime) no planificada que desconectará a los usuarios. Solo se debe usar "inmediatamente" en casos de emergencia crítica.



---

### Parte 4: AWS CLI (Línea de Comandos)

Ahora debes ejecutar los siguientes comandos en tu terminal (asegúrate de tener AWS CLI configurado).

1. Listar instancias (JSON y Tabla) 

* **Comando JSON:**
```bash
aws rds describe-db-instances

```


* **Comando Tabla:**
```bash
aws rds describe-db-instances --query "DBInstances[*].{ID:DBInstanceIdentifier,Engine:Engine,Status:DBInstanceStatus,Clase:DBInstanceClass}" --output table

```


*(Nota: El modificador `--query` ayuda a filtrar columnas específicas para que la tabla sea legible).*

2. Mostrar detalles de tu instancia 
Sustituye `nombre-de-tu-bd` por el identificador real de tu base de datos.

```bash
aws rds describe-db-instances --db-instance-identifier nombre-de-tu-bd

```

3. Crear una instantánea (Snapshot) manual 

```bash
aws rds create-db-snapshot --db-instance-identifier nombre-de-tu-bd --db-snapshot-identifier mi-snapshot-practica3

```

4. Cambiar el tipo de instancia por CLI 
Este comando realiza el escalado vertical nuevamente (por ejemplo, volviendo a `db.t3.micro` o subiendo a otra).

```bash
aws rds modify-db-instance --db-instance-identifier nombre-de-tu-bd --db-instance-class db.t3.micro --apply-immediately

```

---

### Resumen de Capturas de Pantalla Necesarias

Para verificar que has realizado la práctica correctamente, asegúrate de incluir en tu informe:

1. 
**Captura de Almacenamiento:**  Donde se vea el tamaño por defecto y la opción "Habilitar escalado automático de almacenamiento" marcada.


2. **Captura del Cambio de Instancia (GUI):** Una imagen del panel donde se muestra que la instancia ha cambiado de tipo (ej. de micro a small) o el estado "Modifying".
3. **Capturas de la Terminal (CLI):**
* Resultado del comando de listado en formato tabla.
* Resultado de la creación del snapshot.
* Resultado del comando `modify-db-instance` donde se vea el JSON de respuesta indicando `pending-modified-values`.



¿Te gustaría que te ayude a redactar alguna de las explicaciones teóricas con un lenguaje más técnico para tu informe?