Esta práctica consiste en aprender a modificar la configuración de una base de datos Amazon RDS sin tener que reinstalarla, utilizando los "DB Parameter Groups" (Grupos de Parámetros). Algunos cambios se aplican al instante y otros requieren reiniciar la base de datos.

Aquí tienes la guía paso a paso para resolver la práctica y las capturas que debes tomar.

---

### **Parte 1: Preparación**

1.  **Crear la instancia RDS:**
    *   Ve a la consola de AWS -> **RDS** -> **Create database**.
    *   Elige **Standard create** -> **MySQL**.
    *   **Importante:** Fíjate bien en la versión de MySQL que eliges (por ejemplo, *MySQL 8.0.35*), ya que el grupo de parámetros debe coincidir con esta "familia".
    *   Usa la capa gratuita (Free Tier) o Dev/Test para evitar costes.
    *   Ponle un nombre sencillo (ej. `mi-instancia-sql`).
    *   Crea la base de datos y espera a que esté en estado **Available**.

---

### **Parte 2: Desarrollo de la Práctica**

#### **Paso 1: Identificar el grupo actual**
1.  Ve a tu lista de **Databases** y haz clic en el nombre de tu instancia.
2.  Ve a la pestaña **Configuration** (Configuración).
3.  Busca la línea que dice **Parameter group**. Verás que dice algo como `default.mysql8.0`.
    *   📷 **CAPTURA 1:** Haz una captura de esta pantalla donde se vea el grupo de parámetros por defecto asociado a tu instancia.

#### **Paso 2: Crear un nuevo Grupo de Parámetros**
1.  En el menú de la izquierda, haz clic en **Parameter groups**.
2.  Haz clic en el botón naranja **Create parameter group**.
3.  **Parameter group family:** Selecciona la familia que coincida con tu base de datos (si creaste una MySQL 8.0.x, elige `mysql8.0`).
4.  **Group name:** Escribe `mysql-parameter-group-tunombre` (sustituye "tunombre" por tu nombre real).
5.  **Description:** Pon cualquier descripción (ej. "Práctica clase").
6.  Haz clic en **Create**.
    *   📷 **CAPTURA 2:** Haz una captura de la lista de grupos de parámetros donde aparezca el nuevo grupo que acabas de crear.

#### **Paso 3: Modificar los parámetros**
1.  Haz clic en el nombre de tu nuevo grupo de parámetros para entrar.
2.  Haz clic en **Edit parameters** (arriba a la derecha).
3.  Usa el buscador para encontrar y modificar los siguientes valores (tal como pide el PDF):
    *   Busca `slow_query_log` ➔ Cambia el valor a **1**.
    *   Busca `long_query_time` ➔ Cambia el valor a **2**.
    *   Busca `log_queries_not_using_indexes` ➔ Cambia el valor a **1**.
    *   Busca `max_connections` ➔ Cambia el valor a **200**.
4.  Haz clic en **Save changes**.

> *Nota:* El PDF muestra un comando de AWS CLI para hacer esto. Si prefieres usar la terminal, puedes hacerlo, pero la interfaz gráfica es más sencilla para visualizar. Si usas el comando del PDF, ten cuidado porque tiene erratas (`ApplyMethodd` en lugar de `ApplyMethod`).

#### **Paso 4: Asociar el nuevo grupo a la instancia**
1.  Vuelve a **Databases** y selecciona tu instancia.
2.  Haz clic en **Modify** (Modificar).
3.  Desplázate hacia abajo hasta la sección **Additional configuration**.
4.  En **DB parameter group**, cambia el `default.mysql8.0` por tu nuevo grupo `mysql-parameter-group-tunombre`.
5.  Haz clic en **Continue** al final de la página.
6.  En la confirmación, selecciona **Apply immediately** (Aplicar inmediatamente) y pulsa **Modify DB Instance**.
    *   📷 **CAPTURA 3:** Haz una captura justo antes de confirmar, donde se vea que estás cambiando el grupo de parámetros por defecto al tuyo nuevo.

#### **Paso 5: Verificar el estado "Pending Reboot"**
1.  Espera un minuto y vuelve a la pestaña **Configuration** de tu instancia.
2.  Fíjate en el apartado **Parameter group**. Debería aparecer tu nuevo grupo, pero al lado dirá **(pending-reboot)** o **pending restart**.
    *   *¿Por qué?* Porque el parámetro `max_connections` es estático y requiere un reinicio para aplicarse, tal como indica el PDF.
    *   📷 **CAPTURA 4:** Haz una captura de la pestaña Configuración donde se vea el estado "pending-reboot" junto al nombre de tu grupo de parámetros.

#### **Paso 6: Aplicar los cambios (Reiniciar)**
Para que los cambios surtan efecto total, debes reiniciar la instancia.
1.  Con la instancia seleccionada, ve a **Actions** ➔ **Reboot**.
2.  Confirma el reinicio.
3.  Espera unos minutos hasta que el estado vuelva a ser **Available**.
4.  Vuelve a la pestaña **Configuration**. Ahora el mensaje de "pending-reboot" debería haber desaparecido y solo debería salir el nombre de tu grupo en verde (o "In-sync").
    *   📷 **CAPTURA 5:** Haz una captura de la pestaña Configuración mostrando que el grupo de parámetros ya está aplicado y sincronizado (sin mensajes de pendiente).

#### **Paso 7: Verificación final (SQL)**
Ahora debes conectarte a la base de datos para demostrar que los valores internos han cambiado.
1.  Conéctate a tu base de datos (puedes usar un cliente como DBeaver, MySQL Workbench, o conectarte desde una instancia EC2 / CloudShell).
2.  Ejecuta las siguientes consultas SQL (una por una o todas juntas):

```sql
SHOW VARIABLES LIKE 'slow_query_log';
SHOW VARIABLES LIKE 'long_query_time';
SHOW VARIABLES LIKE 'max_connections';
```

3.  El resultado debe mostrar:
    *   `slow_query_log`: ON (o 1)
    *   `long_query_time`: 2.000000
    *   `max_connections`: 200
    *   📷 **CAPTURA 6:** Haz una captura de la terminal o del programa SQL mostrando los resultados de estas consultas.

---

### **Resumen de Capturas a entregar**

1.  **CAPTURA 1:** Pestaña Configuración mostrando el grupo `default` original.
2.  **CAPTURA 2:** Pantalla de creación del nuevo Parameter Group o el listado donde aparece.
3.  **CAPTURA 3:** Pantalla de "Modify DB Instance" seleccionando el nuevo grupo.
4.  **CAPTURA 4:** Pestaña Configuración mostrando el estado **pending-reboot**.
5.  **CAPTURA 5:** Pestaña Configuración tras el reinicio (sin mensajes de pendiente).
6.  **CAPTURA 6:** Resultados de las consultas SQL (`SHOW VARIABLES`) confirmando los valores.